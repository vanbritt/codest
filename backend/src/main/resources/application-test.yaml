spring:
  application:
    name: backend
  datasource:
      url: jdbc:h2:mem:testdb
      username: sa
      password:
      initialization-mode: never
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create